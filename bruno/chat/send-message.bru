meta {
  name: Send Message
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/chat/messages
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "conversationId": "{{conversationId}}",
    "content": "Hola, este es un mensaje de prueba"
  }
}
